# TODO

未解決の設計上の問題。仕様ではないので README には書かない。

## Doltコミットの粒度が「DB全体」になっている

`commitDolt()` は `CALL DOLT_COMMIT('-A', ...)` を呼んでおり、`-A` はその時点で未コミットの
変更を**テーブルを問わず全部**1つのコミットにまとめる。書き込みAPIが同時に走ると、
片方のリクエストのコミットがもう片方の変更を巻き込む。

影響:

- 巻き込まれた側の変更が、巻き込んだ側のユーザーの author とコミットメッセージで記録される。
  `GET /api/repos/:id/commits` に、そのレシピを触っていない人が著者のコミットが並びうる
- `mergePullRequest` は「レシピ本体の更新」と「PRを merged にする記録」で2回コミットしており、
  その間に他のリクエストが入ると余計に混ざりやすい

検討して駄目だったもの:

- テーブルを個別に `DOLT_ADD` しても範囲は絞れない（`api/src/clients/doltClient.js` のコメント参照）
- フォークごとに Dolt のブランチを切る案: ブランチ作成自体は `CALL DOLT_BRANCH()` 1行で、
  コネクション単位の `USE cookhub/<branch>` で書き分けもできる。ただし Dolt のブランチと
  マージはDB全体が単位なので、フォークのフォークのように段が深くなるほど、マージ時に
  無関係なレシピの変更まで突き合わせることになる
- Dolt に `DOLT_FORK` は無い。DoltHub の Fork は `DOLT_CLONE` + `DOLT_REMOTE` の組み合わせで、
  こちらはDB丸ごとが単位なのでブランチよりさらに粒度が粗い
